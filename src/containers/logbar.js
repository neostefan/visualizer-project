import React from 'react';
import styled from 'styled-components';

import codes from '../colors/color';

const Styles = styled.div`
    background-color: ${codes.background};
    border: 0.5px solid ${codes.color};
    height: 30%;
    width: 100%;
`

const Logbar = () => {
    return (
        <Styles>

        </Styles>
    )
}

export default Logbar;